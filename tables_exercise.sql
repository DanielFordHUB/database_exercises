use employees;

show tables;

describe employees;
-- Which table(s) do you think contain a numeric type column? all but dept
-- Which table(s) do you think contain a string type column? all but salaries
-- Which table(s) do you think contain a date type column? all
-- What is the relationship between the employees and the departments tables?

describe dept_emp;
show create table dept_manager;

